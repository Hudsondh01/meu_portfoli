<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Avatar Moreno de Cabelo Cacheado</title>
  <style>
    /* Estilos para o avatar */
    .avatar {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background-color: #ffcc99; /* Cor de pele morena */
      position: relative;
      border: 3px solid #000; /* Contorno */
      overflow: hidden;
    }

    /* Cabelo cacheado */
    .cabelo {
      width: 100%;
      height: 40%;
      position: absolute;
      top: 10%;
      background-color: #4d2c1d; /* Cor de cabelo moreno */
      border-radius: 50%;
      clip-path: ellipse(50% 60% at 50% 50%);
      box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.5);
    }

    /* Corpo do rosto */
    .rosto {
      width: 100px;
      height: 100px;
      background-color: #ffcc99; /* Cor de pele morena */
      border-radius: 50%;
      position: absolute;
      top: 30%;
      left: 50%;
      transform: translateX(-50%);
      border: 3px solid #000; /* Contorno do rosto */
    }

    /* Olhos */
    .olho {
      width: 18px;
      height: 18px;
      background-color: #fff;
      border-radius: 50%;
      position: absolute;
      top: 25px;
    }

    .olho.esquerdo {
      left: 25px;
    }

    .olho.direito {
      right: 25px;
    }

    /* Sorriso */
    .boca {
      width: 50px;
      height: 20px;
      background-color: transparent;
      border-radius: 50%;
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      border-top: 3px solid #000;
    }
  </style>
</head>
<body>

  <div class="avatar">
    <!-- Cabelo -->
    <div class="cabelo"></div>
    
    <!-- Rosto -->
    <div class="rosto">
      <!-- Olhos -->
      <div class="olho esquerdo"></div>
      <div class="olho direito"></div>
      
      <!-- Boca -->
      <div class="boca"></div>
    </div>
  </div>

</body>
</html>
